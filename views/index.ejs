<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Note Taking App</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="main w-full max-w-4xl bg-white shadow-lg rounded-lg p-6">
      <div class="form mb-6">
        <h1 class="text-2xl font-bold mb-4 text-center text-gray-800">
          Create a Task
        </h1>
        <form action="/create" method="POST" class="flex flex-col gap-4">
          <input
            type="text"
            name="title"
            placeholder="Enter your title"
            class="border border-gray-300 rounded-md p-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
          <textarea
            placeholder="Enter your task"
            name="desc"
            class="border border-gray-300 rounded-md p-2 focus:outline-none focus:ring-2 focus:ring-blue-500 resize-none h-24"
          ></textarea>
          <input
            type="submit"
            value="Create Task"
            class="bg-blue-500 text-white font-semibold py-2 rounded-md hover:bg-blue-600 transition duration-200 cursor-pointer"
          />
        </form>
      </div>

      <h2 class="text-xl font-semibold mb-4 text-gray-700">Tasks</h2>

      <!-- Tasks Grid -->
      <div class="tasks grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-5">
        <% if (files.length > 0) { %>
             <% files.forEach((task) => { %>

        <div
          class="task bg-gray-50 border border-gray-200 rounded-lg p-4 shadow-sm hover:shadow-md transition duration-200"
        >
          <h3 class="text-lg font-semibold text-gray-800"><%= task%></h3>

          <div class="flex justify-between aling-center">
            <a href="/files/<%= task %>" class="text-blue-500 hover:underline  inline-block">
            Read more
          </a>
          <a href="/edit/<%= task %>"> Edit File</a>
          <a href="/delete/<%= task %>"> ❌</a>
          </div>
        </div>

        <% }) %> <% } else { %>

        <p class="text-gray-500 col-span-full text-center">
          No tasks available
        </p>

        <% } %>
      </div>
    </div>
  </body>
</html>
